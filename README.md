
## Configuration

Set the `HF_TOKEN` environment variable with your Hugging Face user access token before running scripts that use the Hugging Face API (e.g. `newNew.py`).

```bash
export HF_TOKEN=your_token_here
```
